#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n,a;
    cin >> n >> a;
    vector<int>liczby(n+1);
    for(int i=1;i<=n;++i){
        cin >> liczby[i];   
    }
    vector<vector<int> >ciag(n+1, vector<int>(n+1));
    //elementy,sumy
    for(int i=1;i<=n;++i){
        for(int j=1;j<=n;++j){
            if(ciag)
        }
    }
    return 0;
}