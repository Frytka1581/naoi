#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll r,g,b;
    cin >> r >> g >> b;
    ll maks = 0;
    ll ktory = -1;
    if(maks<r) ktory=0;
    if(maks<g) ktory=1;
    if(maks<b) ktory=2;
    while(maks-1>)
    return 0;
}